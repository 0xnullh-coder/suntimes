{
  "name": "suntimes",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "db:seed": "pnpm --filter @suntimes/shared run db:seed",
    "db:migrate": "pnpm --filter @suntimes/shared run db:migrate",
    "db:push": "pnpm --filter @suntimes/shared run db:push"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.15.0"
}
